ZSH=$HOME/.oh-my-zsh

#need to work on it purging it

# You can change the theme with another one:
# #   https://github.com/robbyrussell/oh-my-zsh/wiki/themes
# ZSH_THEME="lambda-gitster"
#
# # Useful plugins for Rails development with Sublime Text
# plugins=(gitfast last-working-dir common-aliases zsh-syntax-highlighting history-substring-search zsh-autosuggestions)
#
# # Prevent Homebrew from reporting - https://github.com/Homebrew/brew/blob/master/share/doc/homebrew/Analytics.md
# export HOMEBREW_NO_ANALYTICS=1
#
# # Actually load Oh-My-Zsh
# source "${ZSH}/oh-my-zsh.sh"
# unalias rm # No interactive rm by default (brought by plugins/common-aliases)
#
# # Load rbenv if installed
# export PATH="${HOME}/.rbenv/bin:${PATH}"
# type -a rbenv > /dev/null && eval "$(rbenv init -)"
#
# # Rails and Ruby uses the local `bin` folder to store binstubs.
# # So instead of running `bin/rails` like the doc says, just run `rails`
# # Same for `./node_modules/.bin` and nodejs
# export PATH="./bin:./node_modules/.bin:${PATH}:/usr/local/sbin"
#
# # Store your own aliases in the ~/.aliases file and load the here.
# [[ -f "$HOME/.aliases"  ]] && source "$HOME/.aliases"
#
# # Encoding stuff for the terminal
# export LANG=en_US.UTF-8
# export LC_ALL=en_US.UTF-8
# export VISUAL=vim
# export EDITOR="$VISUAL"
# export BUNDLER_EDITOR="$VISUAL"
# # Aliases
# alias alert="osascript -e 'display notification \"Process over\" with title \"Terminal\"'"
# alias comandos='vim ~/Library/Mobile\ Documents/com~apple~CloudDocs/.comandos'
# alias denv="source 'env/bin/activate'"
# alias tenv="source 'tenv/bin/activate'"
# alias dlserve='py manage.py runserver & sleep 2; open http://127.0.0.1:8000/'
# alias eb='~/code/eventbrite'
# alias gotopy3='$HOME/Documents/Learning/python3'
# alias py='python'
# alias vimrc='vim ~/.vimrc'
# alias zshrc='vim $HOME/.zshrc'
# # fixing tmux to adopt same color
# alias tmux='TERM=xterm-256color tmux'
# bindkey -v
